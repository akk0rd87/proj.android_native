# Структура проекта JCross Compose Prototype

## Обзор

Полностью функциональный прототип Android-приложения JCross Nonograms на Kotlin с Jetpack Compose.

## Иерархия директорий

```
jcross_compose_prototype/
│
├── build.gradle.kts              # Конфигурация Gradle
├── settings.gradle.kts           # Настройки проекта
├── gradle.properties             # Свойства Gradle
├── AndroidManifest.xml           # Манифест приложения
│
├── README.md                     # Основная документация
├── INTEGRATION_GUIDE.md          # Руководство по интеграции с C++
├── PROJECT_STRUCTURE.md          # Этот файл
│
└── src/main/
    ├── java/com/jcross/prototype/
    │   │
    │   ├── MainActivity.kt           # Главная активность
    │   │
    │   ├── model/                    # Модели данных
    │   │   └── Models.kt            # Group, Folder, Puzzle, GameField, и др.
    │   │
    │   ├── data/                     # Слой данных
    │   │   └── MockData.kt          # Заглушки для тестирования (TODO: заменить на JNI)
    │   │
    │   ├── navigation/               # Навигация
    │   │   └── Navigation.kt        # Граф навигации и маршруты
    │   │
    │   └── ui/
    │       ├── theme/                # Тема приложения
    │       │   ├── Color.kt         # Цветовая палитра
    │       │   ├── Theme.kt         # Material Theme конфигурация
    │       │   └── Type.kt          # Типография
    │       │
    │       ├── components/           # Переиспользуемые компоненты
    │       │   └── CommonComponents.kt  # SizeCard, FolderItem, PuzzleItem и др.
    │       │
    │       └── screens/              # Экраны приложения
    │           ├── MainScreen.kt    # Главный экран (выбор размера)
    │           ├── GroupScreen.kt   # Список папок в группе
    │           ├── FolderScreen.kt  # Список головоломок в папке
    │           ├── GameScreen.kt    # Экран игры
    │           ├── OptionsScreen.kt # Настройки
    │           └── ThemesScreen.kt  # Выбор тем
    │
    └── res/
        └── values/
            ├── strings.xml           # Строковые ресурсы
            └── themes.xml            # XML темы
```

## Описание модулей

### 📱 MainActivity
- Точка входа приложения
- Инициализирует Compose и навигацию
- Применяет тему JCross

### 🎨 UI Layer

#### Screens (Экраны)
1. **MainScreen** - Выбор размера головоломок (XS, S, M, L, XL)
2. **GroupScreen** - Список папок для выбранного размера
3. **FolderScreen** - Список головоломок в папке
4. **GameScreen** - Игровое поле для решения головоломки
5. **OptionsScreen** - Настройки звука, вибрации, отображения
6. **ThemesScreen** - Выбор цветовой темы приложения

#### Components (Компоненты)
- `SizeCard` - Карточка размера на главном экране
- `FolderItem` - Элемент списка папок
- `PuzzleItem` - Элемент списка головоломок
- `GameCell` - Ячейка игрового поля
- `BottomNavigationBar` - Нижняя панель навигации
- `JCrossTopBar` - Верхняя панель с заголовком

#### Theme (Тема)
- **Color.kt** - Цветовая палитра для каждого размера
- **Theme.kt** - Material 3 тема (светлая/тёмная)
- **Type.kt** - Типографика (шрифты, размеры)

### 🧩 Navigation Layer
- Использует Jetpack Navigation Compose
- Определяет маршруты между экранами
- Передаёт параметры (groupId, folderId, puzzleId)

### 📊 Data Layer

#### Models
- `PuzzleSize` - Enum размеров (XS, S, M, L, XL)
- `Group` - Группа головоломок по размеру
- `Folder` - Папка с головоломками
- `Puzzle` - Отдельная головоломка
- `GameField` - Игровое поле с ячейками и подсказками
- `CellState` - Состояние ячейки (Empty, Filled, Marked)
- `AppTheme` - Тема оформления
- `AppSettings` - Настройки приложения

#### MockDataProvider
**⚠️ TODO:** Заменить на JNI интерфейс для вызова C++ кода

Текущие методы (заглушки):
- `getGroups()` - Получить все группы размеров
- `getFoldersForGroup()` - Получить папки для группы
- `getPuzzlesForFolder()` - Получить головоломки для папки
- `getGameField()` - Получить игровое поле
- `getThemes()` - Получить доступные темы
- `getSettings()` - Получить настройки

## Навигационная схема

```
MainScreen
    ├──> GroupScreen (groupId)
    │       └──> FolderScreen (groupId, folderId)
    │               └──> GameScreen (groupId, folderId, puzzleId)
    │
    ├──> OptionsScreen
    │
    └──> ThemesScreen
```

## Стилистика UI

Использован стиль **"Cards"** с характеристиками:
- ✅ Белые карточки с тенями
- ✅ Цветовое кодирование размеров
- ✅ Прогресс-бары для визуализации прогресса
- ✅ Современные скругленные углы (12-20dp)
- ✅ Просторное расположение элементов
- ✅ Чёткая визуальная иерархия

## Технологический стек

- **Язык:** Kotlin
- **UI Framework:** Jetpack Compose (Material 3)
- **Навигация:** Navigation Compose
- **Минимальная версия Android:** API 24 (Android 7.0)
- **Целевая версия:** API 34 (Android 14)

## Следующие шаги

1. ✅ Создана базовая структура проекта
2. ✅ Реализованы все основные экраны
3. ✅ Добавлена навигация
4. ✅ Созданы заглушки данных
5. ⏳ **TODO:** Интеграция с C++ кодом через JNI
6. ⏳ **TODO:** Подключение реальной логики игры
7. ⏳ **TODO:** Добавление анимаций и переходов
8. ⏳ **TODO:** Оптимизация производительности
9. ⏳ **TODO:** Тестирование на реальных устройствах
